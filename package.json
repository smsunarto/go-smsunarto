{
  "name": "go-smsunarto",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "shorten": "node_modules/.bin/vercel-redirects"
  },
  "devDependencies": {
    "vercel-redirects": "^1.2.0"
  },
  "scripts": {
    "build": "echo \"vercel dev requires a build script & public directory. don't delete\"",
    "shorten": "vercel-redirects"
  },
  "vercel-redirects": {
    "autoPush": true
  }
}
